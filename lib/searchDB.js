var collection = require(__dirname + '/../collection/wordCollection.js').user;

//EXAMPLE testing

/*
var obj = {username: 'iryna', word: "cat", timeStart: 0, timeEnd: 20000, guesses: 8};


collection.updateUser(obj, function (){
	console.log('updated')
})

collection.find({'username': 'iryna'}, function(err, userObj) {
	if (err) console.log (err);
	console.log(userObj);
})
*/
